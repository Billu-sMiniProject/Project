# FitAI — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages

# ── Core API ──────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20        # for file uploads
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1                   # async HTTP for HF/Gemini calls

# ── Image classification ───────────────────────────────────────────────
torch==2.5.1
torchvision==0.20.1
Pillow==11.1.0
timm==1.0.12                    # EfficientNet-B2 pretrained weights

# ── Nutrition lookup ───────────────────────────────────────────────────
rapidfuzz==3.11.0               # fuzzy string matching
google-generativeai==0.8.4      # Gemini fallback

# ── OCR — mess menu board scanning (Feature 8) ────────────────────────
easyocr==1.7.2                  # downloads ~1.5GB model on first run

# ── Agentic meal planning (Feature 6) ─────────────────────────────────
langchain==0.3.14
langchain-community==0.3.14
# HF endpoint is accessed via httpx directly — no extra langchain-hf pkg needed

# ── RAG Q&A (Feature 7) ───────────────────────────────────────────────
chromadb==0.6.3                 # vector store (persists to .chroma_db/)
sentence-transformers==3.3.1    # all-MiniLM-L6-v2 embeddings (~80MB)